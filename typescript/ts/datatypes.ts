let word: string = "Hello";

let num: number = 12;

let choice: boolean = true;

let anything: any = "Any type in here";

let languages: Array<string> = ["Python", "JS"]; // Array of Strings

let numbers: number[]; // array of numbers

let multiple : number | string;

type letters_or_numbers = string | number;
let let_or_num: letters_or_numbers;